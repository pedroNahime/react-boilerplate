# React Boilerplate

## Included scripts

- `build` - runs `webpack` which results in a build to the `./dist` directory
- `dev` - runs `serve` for development
- `test` - runs `jest`
- `format`
- `lint`
